# dependencies
/node_modules

# next.js
/.next
/out

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# database
*.db
*.db-shm
*.db-wal
*.db-journal

# electron build output
dist/
build-resources/

# typescript build cache
tsconfig.tsbuildinfo

# runtime files
*.log
*.pid

# Railway CLI project config
.railway/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# generated test/demo PDFs
/job-pdfs/
/scripts/generate-job-pdfs.js

# Agent tool worktree snapshots (left behind by Agent + isolation="worktree")
.claude/

# CV-extraction failure log: written at repo root by scripts/extract-cv-text.mjs
# on each run. Per-run artifact, not source. Safe to delete anytime.
/extract-cv-failures.json

# One-off iPhone debugging dump from 2026-05-17 (devicectl + 60s syslog).
# Kept locally for reference; not part of the app. TODO: delete once the
# iPhone-side issue it captured is fully closed out.
/iphone-diagnostics/
